_site/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/
Gemfile.lock
.DS_Store
node_modules/
package-lock.json

# Plaintext source for the encryption pipeline — NEVER commit.
# Only the output in `encrypted/` should be tracked.
src/
.encrypt-cache.json
