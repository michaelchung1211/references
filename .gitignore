_site/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/
Gemfile.lock
.DS_Store
node_modules/
package-lock.json

# Plaintext source for encrypted content — NEVER commit.
# Only the ciphertext in `encrypted/` should be tracked.
_plaintext/
.encrypt-cache.json
